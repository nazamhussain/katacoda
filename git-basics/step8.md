**Objective: View the repository's commit history.**

Use the `git log` command to view the repository's commit history.

## View Commit History

 * `git log`{{copy}}


## View Condensed Commit History

 * `git log --oneline`{{copy}}
